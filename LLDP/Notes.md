LLDP is an industry standard protocol (IEEE 802.1AB)
It usually disables on cisco devices by default, so it must be manually enabled. 
A device can run CDP and LLDP at the same time. 
LLDP messages are periodically sent to multicast MAC address 0180.C200.000E.
By default LDP messages are sent once every 30 seconds.
By default, the LLDP holdtime is 120 seconds.

We should turn of LLDP is switch. 
